import { title, primaryColor } from "assets/jss/material-kit-react.jsx"

const contactStyle = {
  nameSocial: {
    ...title,
    color: primaryColor,
    textAlign: "center",
  },
}
export default contactStyle
